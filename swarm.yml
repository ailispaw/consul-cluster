swarm: &swarm
  image: swarm:latest

create:
  <<: *swarm
  command:
    - create

manager:
  <<: *swarm
  ports:
    - "2380:2375"
  command:
    - manage
    - token://${TOKEN}

agent:
  <<: *swarm
  command:
    - join
    - --addr=${NODE_IP}:2375
    - token://${TOKEN}
